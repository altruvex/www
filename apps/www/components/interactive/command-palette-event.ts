/** Event name the nav trigger dispatches to open the command palette. Kept in
 * its own module so the trigger doesn't pull the palette into its bundle. */
export const COMMAND_PALETTE_EVENT = "altruvex:command-palette";

import { css, PALETTE } from "@repo/ui/palette";

/** PWA manifest colors, from the one palette (@repo/ui/palette). */
export const MANIFEST_THEME_COLOR = css(PALETTE.light.brand);
export const MANIFEST_BACKGROUND_COLOR = css(PALETTE.light["n-0"]);
